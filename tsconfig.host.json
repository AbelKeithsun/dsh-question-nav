{
  // Host program: the cordis plugin half + the questionIndex projection unit
  // and their tests. Separate from the client program because
  // @deepseek-ai/dsh-session merges `Context.sessions` with the host
  // SessionStore service type, incompatible with the browser ISessions merge.
  "extends": "./tsconfig.base.json",
  "include": [
    "src/index.ts",
    "src/projection.ts",
    "src/core/question-entry.ts",
    "tests/projection.spec.ts"
  ]
}
